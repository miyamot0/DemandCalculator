<Window x:Class="small_n_stats_WPF.Views.OmnibusTauWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:local="clr-namespace:small_n_stats_WPF"
        ResizeMode="NoResize"
        mc:Ignorable="d"
        Title="Omnibus Tau-U Calculator" 
        Height="450" 
        Width="450"
        xmlns:settings="clr-namespace:small_n_stats_WPF.Properties"    
        Top="{Binding Source={x:Static settings:Settings.Default}, Path=OmnibusTauTop, Mode=TwoWay}" 
        Left="{Binding Source={x:Static settings:Settings.Default}, Path=OmnibusTauLeft, Mode=TwoWay}">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding ViewLoadedCommand}"/>
        </i:EventTrigger>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding ViewClosingCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="4"
            FontSize="18"
            Text="Tau-U Omnibus Calculations"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
        </TextBlock>

        <Label
            Grid.Row="1"
            Grid.RowSpan="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Content="Name of Series (Optional)"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            
        </Label>

        <TextBox
            Grid.Row="1" 
            Grid.Column="2"
            Grid.ColumnSpan="2"
            Margin="10"
            Text="{Binding TauName}">
        </TextBox>

        <Button Name="baselineButton" 
                Grid.Row="2" 
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Margin="10"
                Content="Baseline"
                Command="{Binding LoadBaselineDataCommand}">
        </Button>

        <TextBox
            Name="baselineRange"
            Grid.Row="2" 
            Grid.Column="2"
            Grid.ColumnSpan="2"
            IsReadOnly="True"
            Margin="10"
            Text="{Binding BaselineRangeString}"
            Background="{Binding BaselineBackGround}">
        </TextBox>

        <Button Name="interventionButton" 
                Grid.Row="3" 
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Margin="10"
                Content="Intervention"
                Command="{Binding LoadInterventionDataCommand}">
        </Button>

        <TextBox
            Name="interventionRange"
            Grid.Row="3" 
            Grid.Column="2"
            Grid.ColumnSpan="2"
            IsReadOnly="True"
            Margin="10"
            Text="{Binding InterventionRangeString}"
            Background="{Binding InterventionBackGround}">
        </TextBox>

        <ListView
            Name="listViewContents"
            Grid.Row="4"
            Grid.RowSpan="3"
            BorderBrush="Transparent"
            BorderThickness="0"
            Grid.Column="0"
            Margin="10,0,10,0"
            Grid.ColumnSpan="4"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            ItemsSource="{Binding TauUHolder}">
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Delete" 
                              Command="{Binding RemoveItemCommand}"
                              CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}" />
                </ContextMenu>
            </ListBox.ContextMenu>
            <ListView.View>
                <GridView AllowsColumnReorder="False">
                    <GridViewColumn Header="Tau Measures for omnibus comparison (CTRL + Click to select)"  
                                    DisplayMemberBinding="{Binding Path=Name}" 
                                    Width="500"/>
                </GridView>
            </ListView.View>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="IsSelected" Value="{Binding IsChecked, Mode=TwoWay}" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <!-- "IsSelected" isn't, IntelliSense is showing the members of the Window's ViewModel -->
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Button Name="calculateButton" 
                Grid.Row="7" 
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Margin="10"
                Content="Check Baseline Trend"
                Command="{Binding CheckBaselineCommand}">
        </Button>

        <CheckBox
            Name="correctBaselineBox"
            Grid.Row="7"
            Grid.Column="2"
            Grid.ColumnSpan="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Content="Correct Baseline"
            IsChecked="{Binding CorrectBaseline}">

        </CheckBox>

        <Button Name="compareButton" 
                Grid.Row="7" 
                Grid.Column="3"
                Grid.ColumnSpan="1"
                Margin="10"
                Content="Calculate"
                Command="{Binding CompareBaselineCommand}">
        </Button>

        <Button Name="omniButton" 
                Grid.Row="8" 
                Grid.Column="0"
                Grid.ColumnSpan="4"
                Margin="10"
                Content="Combine Selected Into Omnibus Tau"
                Command="{Binding CalculateOmnibusCommand}">
        </Button>
    </Grid>
    </Window>
